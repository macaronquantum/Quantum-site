{
  "name": "quantum-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "vite preview --port $PORT --host"
  },
  "dependencies": {
    "vite": "^4.4.0"
  }
}
